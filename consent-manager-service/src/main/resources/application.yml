server:
  port: 9006

spring:
  application:
    name: CONSENT-MANAGER-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9000/eureka
    instance:
      hostname: localhost